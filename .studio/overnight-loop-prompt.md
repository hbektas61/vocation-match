/ralph-loop:ralph-loop Use studio-autopilot and act as the Vocation Match project-orchestrator. Start a new overnight pilot-hardening program from the current origin/main state. Read CLAUDE.md, the global Studio PLAYBOOK.md, and every .studio file. First replace the closed program in .studio/agent-plan.md with this new four-phase program and record that the real-device pass remains externally deferred under D-015. Phase 1 must replace arbitrary profile photo URLs with secure Supabase Storage uploads, owner-controlled writes, safe authenticated reads, cleanup, mobile UI integration, and focused tests. Phase 2 must implement complete in-app account deletion with confirmation UX, server-side ownership enforcement, auth and data cleanup, local session removal, tests, and honest failure handling. Phase 3 must make email-confirmation readiness complete through correct local configuration, unconfirmed-email mobile states, configuration verification, and hosted-project instructions, then resolve S-004 with unambiguous swipe and match room attribution through a safe migration, typed client updates, concurrency tests, and regression tests. Phase 4 must perform a full pilot-hardening pass over security, privacy, abuse resistance, accessibility, lifecycle, offline behavior that can be simulated locally, migration replay, client and database contract drift, dependency health, performance smoke checks, and documentation. At every intermediate phase boundary, run all relevant database tests, concurrency checks, contract checks, TypeScript checks, lint, mobile tests, and supported builds. Repair failures and every valid critical or high review finding. Update durable Studio evidence, commit, integrate into main, push origin/main directly, and immediately continue without asking the owner, opening a pull request, stopping, or emitting the completion promise. Work only inside this repository and temporary test directories. Normal repository edits, dependency installation, local containers, migrations against disposable local databases, tests, builds, commits, and direct main pushes are permanently authorized. Never access unrelated personal files. Never use real customer data. Never spend money. Never deploy production. Never submit to stores. Never use or create production secrets. Never delete production data. Never force-push or rewrite history. If real hardware, hosted Supabase settings, credentials, or another external dependency is unavailable, record the exact deferred verification and immediately continue every safe local task. Do not wait for owner input when a safe locally verifiable alternative exists. Output the completion promise only after all four locally actionable phases are verifiably complete, all reviews pass, origin/main contains every verified checkpoint, the worktree is clean, and every external check is honestly handed off. --max-iterations 100 --completion-promise VOCATION_OVERNIGHT_HARDENING_COMPLETE
