# Decisions

These decisions are owner-approved and must not be silently changed.

| ID | Date | Decision | Reason | Revisit when |
| --- | --- | --- | --- | --- |
| D-001 | 2026-07-24 | Upcoming access uses self-declared hotel and stay dates; no reservation proof is collected. | The owner wants minimum friction and minimum sensitive data. | Fraud or hotel-hopping data shows material harm. |
| D-002 | 2026-07-24 | Here Now access requires only a recent foreground check within 500 meters of the selected hotel. | Proximity is sufficient for the intended room. | Abuse data justifies stronger controls. |
| D-003 | 2026-07-24 | A user can have exactly one active hotel at any moment. | This is the primary scope and abuse constraint. | Never silently; owner decision required. |
| D-004 | 2026-07-24 | Changing hotels immediately closes discovery access in the previous hotel. Existing matches/chats may remain. | Preserve the one-hotel rule without destroying social connections. | User safety evidence suggests chat expiry. |
| D-005 | 2026-07-24 | Exact coordinates and exact live distances are never shown to users or placed in analytics. | Privacy and stalking-risk reduction. | Never loosen without explicit privacy review. |
| D-006 | 2026-07-24 | Payments and premium access are deferred to the next product phase. | Validate the core loop before monetization. | Core pilot flow is working and measured. |
| D-007 | 2026-07-24 | Product copy must say “self-declared upcoming” and “near the hotel now,” not “verified reservation/guest.” | Prevent misleading trust claims. | A real hotel or reservation integration exists. |
| D-008 | 2026-07-24 | MVP is 18+ and includes block/report from the first usable build. | Social matching requires baseline safety. | Never remove. |
| D-009 | 2026-07-25 | Claude Studio may integrate verified increments into `main` and push `origin/main` directly without asking or opening a PR. Temporary local worktree branches are allowed for isolation. | The owner wants continuous delivery without routine pull requests. | Owner explicitly revokes authorization. |
| D-010 | 2026-07-25 | The autonomous build runs four consecutive phases and pauses only after the complete MVP systems program, not after an intermediate milestone. | The owner wants three to four phases of uninterrupted implementation. | Owner explicitly changes the program boundary. |
| D-011 | 2026-07-25 | Blocking is reversible: `unblock_user()` plus a blocked-list in Settings. Unblocking does not restore the match the block ended. | Backlog R-002 flagged that an irreversible block is a UX defect an accidental tap cannot undo. Reversibility is the standard, safe default; the one-way part (no match resurrection) keeps the safety guarantee. | Abuse data shows unblocking is used to harass. |
| D-012 | 2026-07-25 | One swipe decision per pair, stored permanently. A repeat swipe is a no-op that returns the existing outcome rather than an error. | Idempotency is what makes the endpoint safe to retry over a flaky mobile connection, and it removes "swipe again to change your mind" from the MVP surface. | Users ask for an undo, or a rewind becomes a premium feature. |
| D-015 | 2026-07-25 | The on-device test pass is deferred to the next phase as an accepted, recorded risk. The MVP systems program closes without it. | No simulator or device toolchain exists on the build machine, and installing one is owner work. Everything a bundle can prove is proven; keeping the program open changed nothing. | The owner installs Xcode / the Android SDK, or connects a phone with Expo Go. Do this before any pilot with real users, not before the next code milestone. |
| D-014 | 2026-07-25 | Profile photos will be served from our own storage rather than an arbitrary URL. Until that exists, `photo_url` is capped at 2048 characters and must be https. | A card is shown in discovery without any interaction, so a self-hosted image URL is a passive beacon: the profile owner learns the IP and timing of everyone who merely saw them. On an app whose whole promise is not revealing who is near whom, that is the wrong default. Raised by the security audit. | The storage bucket exists and the client stops accepting arbitrary URLs. |
| D-013 | 2026-07-25 | Moderation escalates automatically at three distinct reporters (a FLAGGED action), and an actioned report suspends the account, which removes it from every room and stops it messaging. | The MVP needs a safety response that does not depend on someone watching a queue in real time. | Volume makes the threshold noisy in either direction. |

| D-016 | 2026-07-25 | A repeat swipe, and a photo read, never depend on where the other person is at that moment. A decision already made is answered from storage; a photo stays visible to someone who was already shown the card, whether or not its owner is checked in right now. | The pilot-hardening audit found that both endpoints answered from the target's *live* room eligibility, and a user id is public to everyone who has seen a card. Polling either one told you the moment a specific person arrived near the hotel and the moment they left — a live presence feed on someone who, in the swipe case, you had already been removed from the deck of. That is the behaviour D-005 exists to prevent, and it also broke D-012: a retry after a dropped response failed if the other person had moved in between. | Never loosen without a privacy review. Note what this does not close: the fast path does measurably less work than a first swipe, so a precise enough timing measurement could still tell the two apart. That is a far weaker signal than the one it replaced — it says whether you have swiped on someone, not where they are — and closing it would mean making every call do the slow path's work. Worth knowing before this ever faces a more adversarial setting than an invited pilot. |

| D-017 | 2026-07-26 | The way into the app is one onboarding wizard whose current step is *derived* from real state — session, profile, active hotel — rather than stored anywhere. Only the "show the teaching cards" flag lives in memory, and a cold start begins with it false. A profile without an active hotel resumes conservatively at bio: repeating optional steps is preferable to silently dropping them. | Storing a cursor means two sources of truth about where somebody is, and the one on the device is the one that can be wrong. Deriving it also gives the requirement "a finished onboarding must not reappear" for free, with nothing written to disk. | A step is ever added that the server has no way to observe. |
| D-018 | 2026-07-26 | `profiles.interests` exists: up to five self-chosen entries of 1–24 characters, readable on a discovery card and never used as a filter. Omitting the field from a profile write means "leave it alone", not "clear it". | The domain model has carried `interests` since the first milestone with nothing behind it, and the onboarding step that asks for them would otherwise throw the answer away. Bounded because the column is free text, and an unbounded array of unbounded strings is a place to put a payload. Not searchable, because something to read on a card is not the same as a facet to be targeted with. | Interests are ever wanted as a matching signal — that is a different privacy question. |
| D-019 | 2026-07-26 | Account entry is phone-only SMS OTP. The same six-digit code flow creates or restores an account; email/password UI and email sign-up are removed. Phone numbers stay in Supabase Auth and are never copied into profiles, discovery, analytics, or logs. | The owner expects people to use the app while travelling and explicitly chose the lower-friction phone path. One flow also avoids account enumeration and removes password handling from the app. | A reviewed identity migration or an additional sign-in method is explicitly approved. |

| D-020 | 2026-07-26 | The palette is one brand colour, `#E1C4FF`, on a white ground. Because that colour measures 1.55:1 against white it is never a control edge, never text on white, and never the only carrier of a state — it always appears with a darker sibling of the same hue (`#7B4FA8`, 5.96:1), with ink on top of it (11.68:1), or with a word or mark beside it. | The owner specified the hex and specified white surfaces. Both are honoured exactly; what could not be honoured is the implicit assumption that a colour this light can do a boundary's job, so the palette is arranged so that it never has to. | The brand colour changes to one that clears 3:1 on white, at which point the companion values can be simplified away. |
| D-021 | 2026-07-26 | The focused input border is exactly `#E1C4FF`, and the border alone changes: its weight goes from 1.5 to 2.5. **Amended same day by the owner**, who saw the filled version in the app and asked for the fill to go — the earlier form of this decision added a lavender ground and a `#7B4FA8` outer ring. | The owner asked for that precise border colour, and then for it to be the whole signal. The weight change is what keeps the state perceivable for someone the hue does not register for, so the state is still not colour-alone. | Never weaken to colour alone — the weight change is the floor. |
| D-022 | 2026-07-26 | Turkish phone entry is a fixed, non-editable `+90` beside the box; the editable value is ten national digits and E.164 is produced once, at the API call. Turkey-only parsing lives in `phoneTr.ts`, separate from the generic E.164 helpers in `phone.ts`. | A prefix that lives in the value can be half-deleted, and a number with a mangled country code fails at the provider rather than in the field. Keeping the strict, single-country parser separate lets the onboarding field reject a landline without narrowing what the rest of the app is able to accept. | Another country is supported, which needs a country picker rather than a second hardcoded prefix. |
| D-023 | 2026-07-26 | `show_me` filters discovery in **both** directions: A sees B only when A's answer matches B's gender and B's answer matches A's. WOMEN and MEN are the only two values the filter knows, so a person whose gender is neither is reachable only by someone asking to see everyone. Sexual orientation is never a filter — it is shown when its owner publishes it, and nothing more. | Filtering one way lets one person's preference override the other's. The two-value limit is a real exclusion and is written down rather than hidden: the alternative was folding every other gender into one of two buckets, which is worse, and guessing is not available. Orientation stays out of matching because inferring who somebody wants to meet from how they describe themselves is exactly the assumption this product should not make. | Reconsider when there is a way to let people outside the two say who should be able to find them, rather than the app deciding for them. Any change needs a privacy review. |
| D-024 | 2026-07-26 | A profile is finished when the server says so: `onboarding_completed_at`, set only by `complete_onboarding()`, which requires a gender and a show-me answer. The column is absent from every client grant. A profile without the mark is a draft and is excluded from discovery. Gender and show-me are required; orientation, passions and a photo are not. | "Has a profile row" stopped meaning "finished" the moment the profile was built across several screens — a row exists from the birthdate step onward, and somebody who closed the app on the gender question would otherwise have been a complete, discoverable profile with no say in who saw them. Photo stays optional because a required photo is a barrier to the people most careful about being seen, and the brief asked for the decision to be recorded either way. | A required answer is added, which means both a migration and a completion rule change. |
| D-025 | 2026-07-26 | Choosing a hotel is not part of finishing onboarding. A complete profile enters the app with no active hotel; the hotel is asked for when somebody reaches for something that needs one. | The hotel is a fact about right now and changes often, while the profile is made once. Blocking entry on it also meant a returning user who had left their hotel was thrown back into onboarding. | The product ever has content that is meaningless without a hotel on the first screen. |
| D-026 | 2026-07-26 | A profile holds up to nine ordered photos in `profile_photos`; `profiles.photo_path` stays derived from slot 1. **Amended same day by the owner**: a discovery card now carries the whole ordered set (`photo_paths`), because one photo on a card is not believable — which means the photo count and paths are visible to exactly the people who can see the card. The storage read policy needed no change: `app.may_view_photo` always authorised per owner, not per path, so sharing names with the people the policy already admits is the policy working. The set's management surface (add/remove/reorder) remains owner-only. | The original owner-only rule protected the count from the room; the owner weighed that against a one-photo card reading as fake and chose believability, explicitly. | The card must never carry photos of somebody the viewer could not see through `may_view_photo` — the feed's own eligibility filters are what guarantee that today. |
| D-027 | 2026-07-26 | Photos are reordered by hold-and-drag; the caption says so and is now true. **Amended same day by the owner**, who used the arrow-button version and asked for the photos back — no arrows, no overlay band on the pictures. The assistive path survives invisibly: each tile carries "move earlier/later" accessibility actions, which is how a screen reader reorders since a drag target is not something VoiceOver can hand you. The hold-before-lift (180 ms) is what keeps a scroll that begins on a photo being a scroll. | The first version chose visible controls precisely because a described-but-absent gesture is a caption for a different app; once the owner asked for the gesture, the honest move was to build it and keep the assistive equivalent, not to argue for the buttons. | The accessibility actions are the floor — the drag must never become the only way to reorder. |
| D-028 | 2026-07-26 | Local files are read with `expo-file-system`'s `File`, never `fetch('file://…')`. | `fetch` reaches the network stack: on Android that is OkHttp, which has no handler for the `file` scheme, so every upload failed there. On iOS it succeeds and then `arrayBuffer()` routes through React Native's `FileReader`, which asks the native side for a base64 data URL and decodes it in JS — an extra full copy of a multi-megabyte image, in string form, on the JS thread. The dependency is added at the version `expo` pins for SDK 54; it is not an SDK change. | Never reintroduce `fetch` on a `file://` URI. |
| D-029 | 2026-07-26 | The hotel catalogue is materialised lazily from OpenStreetMap. The `hotel-search` edge function searches the local table first; when it answers thinly it asks Nominatim (Turkey-only, hotel-type places), writes the hits through `upsert_hotel_from_provider('osm', …)`, and searches again. The client falls back to the catalogue-only RPC when the function is unreachable. The hotel screen carries the ODbL attribution line. | The owner is right that hand-seeding every hotel does not scale — and the schema always said the table was a provider-fed cache; the seed file was just the only provider it ever had. OSM over a commercial places API because of the licence: ODbL permits storing the data with attribution, while Google's terms forbid caching place data beyond an ID — incompatible with a product whose design is "activate a hotel that lives in our table". The table grows only with what people actually search, bounded by the real world, not by traffic. | A commercial provider is licensed, or Nominatim's 1-request-per-second policy stops fitting the traffic — that is the point to put a queue or a paid geocoder in the function. |
| D-030 | 2026-07-26 | The app speaks English and Turkish. The choice is offered on the first screen (each option labelled in its own language), changeable later in Settings, and persisted on the device. `COPY` is a live module binding switched by `setLocale`; English defines the shape and Turkish must match it key for key, so a sentence cannot ship in one language only. The D-007 trust-copy audit runs over both languages, with Turkish stem-matching, because a promise that holds in English and breaks in Turkish is broken. | The pilot is Turkish; the codebase was English-only. A proxy over the copy object was rejected on a real ground: `Object.entries` cannot see through a proxy with an empty target, and that is exactly how the trust audit walks the sentences — the mechanism must be visible to its own auditor. | A third language arrives — the pattern extends by one file that the compiler forces to completeness. |
| D-031 | 2026-07-26 | Push notifications exist, in exactly two kinds. MESSAGE: the sender's name and a fixed sentence — never the message body, because lock screens have readers. ROOM_NEW: "somebody new at your hotel", nameless, sent to the room-eligible people at that hotel, at most once per person per hotel per six hours. Triggers queue transactionally in SQL (pgTAP-proven); pg_cron + pg_net drain the queue to Expo's push API every minute. Tokens are device credentials: owner-only rows, registered with the device's language after onboarding completes, re-registered on language change, removed before sign-out. No token → no queue row. No client can read the queue. | The owner asked for message and room-arrival notifications. The privacy lines are the product's own: message content and arrival identity are for inside the app, where every existing rule (blocks, eligibility, visibility toggles) already governs them — a push is a knock on the door, not the letter itself. | Adding a notification kind, changing the dedupe window, or putting any personal content in a push body — each is a decision, not a tweak. |
| D-032 | 2026-07-26 | The hotel card tells you how many people are in each room — but only when the crowd hides the individual. `hotel_room_counts` returns the exact number of other eligible people at five or more, and null below; null renders as nothing at all, never "a few" or "somebody", because at one person even "somebody is here" is a presence leak. The count ignores show_me in both directions (a room's population is a fact about the room), never includes the caller, drops suspended and incomplete accounts, and is per active hotel only. Owner intent, recorded not built: this signal becomes a premium perk when the premium phase opens; until decisions.md advances that phase there is no entitlement, no billing, and the count is visible to every account. | The owner asked for the count with a threshold ("eşikle göster") and named premium as its eventual home. Five is the smallest crowd in which no one is anyone. | Changing the threshold, adding any below-threshold wording, or gating the count behind premium — each reopens this decision. |
| D-033 | 2026-07-27 | The brand is **Vacation Match** — with an a. The owner settled the open Vocation/Vacation question while approving the designer's welcome screen ("vacation match yaz, vocation değil, o yanlış olmuş"). Every user-visible string, the app display name and the permission prompts now say Vacation Match; the repository name, slug and staging project ref keep their old spelling because renaming infrastructure buys nothing and risks the pipeline. | The owner said so, in exactly those words. | Renaming the repo/slug/project ref, or changing the brand again. |
| D-034 | 2026-07-27 | The pilot's hotel-search region is Türkiye plus Cyprus (`countrycodes=tr,cy`). The island carries the KKTC resorts (Cratos, Elexus, Merit…) a Turkish holiday audience expects; searching them returned nothing while the region was tr-only. | Owner: "kktc'yi de ekle, tr,cy yap". | Adding further countries, or narrowing back. |
| D-035 | 2026-07-27 | The Upcoming room is scoped to the caller's own declared dates: the deck shows only people whose stay crosses yours, inclusive at the edges (a checkout day and a checkin day are one shared day). The D-032 headcount follows: once you have declared, the Upcoming number counts only the people you could actually meet; before you declare, the whole room. This closes the deliberate breadth recorded at the room's birth ("left broad on purpose; one where clause changes it"). | Owner: everyone in this room declares dates, so everyone should meet the people inside their own window. | Widening back, changing edge inclusivity, or scoping Here Now similarly. |

| D-036 | 2026-07-28 | **Premium entitlement exists; billing still does not.** The rules, all server-enforced: a free member in the Upcoming room gets 3 likes and 5 passes, counted per hotel from stored swipes (a new hotel starts a fresh allowance; a replayed swipe never spends one); the Here Now room is Premium-only — the premium test sits inside `app.room_eligible` itself, so feed, headcount, my_rooms and swipe-target checks agree in the same instant, and `record_presence_check` refuses a free member before ever taking a location. Entitlement is one operator-set column, `profiles.premium_until` — readable by its owner, writable by no client — surfaced as `OwnProfile.isPremium`. Refusals travel as the project-private SQLSTATE `PP001` → client `PREMIUM_REQUIRED`; the room reason is `PREMIUM_ONLY`. No paywall, no purchase flow, no RevenueCat: buying Premium is an explicitly open decision. The owner's "premium can message directly without a match" is accepted and staged as its own next slice (it needs a pre-match message model). | Owner (2026-07-28): "kullanıcı premium değilse rezervasyon odasında maksimum 3 like atabilmeli ve anca 5 kişiyi geçebilmeli (toplam), premiumsa hem doğrudan mesaj atabilir hem de sınırsız like atabilir, premium değilse anında odasına giremez, buraya sadece premium kullanıcılar girebilir". | Changing either allowance number or its per-hotel scope, gating anything else behind premium (the D-032 count is still ungated), opening any purchase flow, or building the direct-message perk. |

| D-037 | 2026-07-28 | **The anchor place is a vacation venue, not only lodging — and the search forgives real typing.** The provider filter admits OSM `bar`, `nightclub`, `beach_resort` and named public beaches (`natural=beach`, same day: "beachleri kesin almalıyız") alongside hotel types, because the summer crowd gathers at beach clubs (the owner's case: Before Sunset, Alaçatı, tagged `amenity=bar`); the filter's job stays "named venues in, bare geography out". `search_hotels` adds squashed matching (joined-up words) and token matching (at most one stray word in a multi-word query); Nominatim gets one last-word-dropped retry when empty-handed. | Owner: "Alaçatı'daki Before Sunset Beach çıkmıyor, oralarda çok yoğunluk var" — and the observed misses "beforesunset" / "before sunset beach" on a row the catalogue already held. | Admitting broader amenity types (restaurants, cafés), any fuzzier matching, or a commercial places API as the discovery source. |

| D-038 | 2026-07-28 | **The region pool.** When the own-venue deck has fewer than five unswiped people, discovery continues with real people anchored at venues within 15 km — each card labelled with the neighbour's venue name ("Lara Dunes Club · çevrede"). D-035 date overlap holds across the region; Here Now still needs premium and a fresh in-range check at the person's own venue; the swipe endpoint recognises exactly the same region the deck shows; the room headcount never absorbs the region. One active venue per person and the no-coordinates/no-distance promise are untouched — the single new disclosure is the venue name on the card, which is the honesty of the feature. The invite mechanic was proposed and rejected by the owner ("davet saçma — tatil rezervasyonu davetle olmaz"); this replaces it. | Owner approved the design 2026-07-28 ("tamamdır yap") after rejecting fake profiles: holiday social life happens at the scale of the town, so the pool should too. Radius 15 km and thin-threshold 5 were offered and accepted. | Changing the radius or threshold, labelling region rows any less clearly, extending the region to counts or notifications, or reviving either rejected idea (invites, fake profiles). |

| D-039 | 2026-07-28 | **"Çevremde" — venue check-ins, free for everyone.** The Swarm-shaped feature the owner asked for, built on the lessons of its ancestors: you check in to a *venue* (never a coordinate), verified by a one-time foreground reading within the same 500 m the rooms use; the check-in lasts 3 hours, one per user, replaced not accumulated — no movement history exists; while it lasts you see and are seen by people checked in within 1 km, cards labelled with their venue's name. Mutuality is structural (the "Girls Around Me" lesson): there is no way to scan a street without standing on it, on the record. NEARBY is a third room in the swipe/match vocabulary; matches and chat work unchanged. No premium anywhere in it — this is the free tier's heart; the D-036 allowance deliberately does not apply (global rate limits still do). Requires no active hotel. | Owner (2026-07-28): "swarm foursquare gibi check-in atacak… 1 km etrafında var olanlar gösterilecek, bu bedava olacak her usera" — accepted with the venue-anchor, mutuality and expiry shape offered in review of Happn/Swarm/Girls-Around-Me precedents. | Changing the radius, duration or verification; adding check-in history; showing anyone without a live mutual check-in; adding premium gates or like-limits to NEARBY; extending ROOM_NEW notifications to check-ins. |

| D-040 | 2026-07-29 | **The information architecture matches the mental model.** Bottom bar is now Tatilim · Çevremde · Keşfet · Mesajlar · Ayarlar. "Tatilim" merges the old Hotel and Rooms tabs — choose a hotel and decide (dates or proximity) on one screen, with no auto-redirect after activation; the two hotel features appear as cards even before a hotel exists, honestly locked. "Çevremde" is its own tab: it never belonged among hotel rooms. Display names: Yaklaşan → **Tatilden Önce**, Şu An Burada → **Oteldeyim**, Çevremde unchanged; API values (UPCOMING/HERE_NOW/NEARBY) untouched. Keşfet gains a context line (hotel + your dates, hotel + minutes left, or venue + time left), a `source` navigation param, and a present-tense default order (Çevremde → Oteldeyim → Tatilden Önce). R-003's self-closing rooms moved with the cards. | Owner (2026-07-29), with a written IA spec: "Çevremde bir otel odası değil… uygulamayı daha basit ve bariz yapmalıyız". | Renaming again, re-splitting Tatilim, reviving auto-redirect, or adding a fourth discovery surface. |

| D-041 | 2026-07-29 | **The Çevremde screens are the designer's, pixel-faithful, and venues know their kind.** Three mocks implemented birebir: the intro (headline, how-it-works bullets, the bar photograph cropped from the mock, vivid find-places button, conditional expired-check-in card, privacy card, palm–heart–cocktail flourish), the around-you list (brand line, locate button, search pill, venue rows with category icons and chips), and the active check-in (venue photograph with licence credit, green active chip, clock line, three actions, safe-and-free card). To power the chips honestly, `hotels.venue_kind` (hotel/cafe/restaurant/bar/beach/area) is written by every provider path and returned by search, nearby and my_checkin. Deliberate departures, each because the control would lie: no unread dot on the inbox tab (no read-state), no heart/favourite button on the venue photo (no favourites), and the two mock sentences that misdescribed the feature ("Konumun görünür", "Profilin gizli kalır") replaced with what it actually does. The tab bar's Tatilim icon became the mock's suitcase. | Owner (2026-07-29): "attığım ekranların birebir aynısı, ikonlarından her şeyine kadar." | Reviving any omitted control without its feature, changing the kind vocabulary, or letting a category chip guess. |

| D-042 | 2026-07-29 | **The radius is an internal rule, not user copy.** Every user-facing "500 m" is gone: the Oteldeyim and Çevremde explainers say "a one-time location check confirms you are at the hotel / at the place", too-far messages say the check could not find you there, and the trust-copy audit now *forbids* metre figures in the explainer instead of requiring them. The 500 m verification itself (D-002) is unchanged on the server — the user consents to a check, not to a geometry lesson. The same pass finished the D-040 rename inside sentence copy: no string says "Şu An Burada" or "Yaklaşan" any more. | Owner (2026-07-29), on the Oteldeyim screen: "kullanıcıya 500 m demek saçma — konumu kendi rızasıyla atıyor, rızayı aldıktan sonra gereksiz." | Putting any radius/metre figure back into user copy, or changing the server radius itself (that is D-002's row). |

| D-043 | 2026-07-29 | **The "rendevuu" palette trial.** The lavender is retired app-wide for the owner's new set: navy #0F1B3D and ink #1A1A2E do the reading; pink #EC4899 is the strong brand (control edges at 3.9:1, washes, chips' tones); lightPink #F472B6 is the brand fill; the one primary-action treatment is the owner's gradient gold #FBBF24 → coral #FB7185 → pink #EC4899 (pressed: the lighter ramp), painted inside Button, BigActionButton and the Çevremde hero CTA. Labels on the gradient are ink, never white — the gold end cannot carry white, and the theme test now proves the label against the gradient's *worst* stop. All hardcoded purples swept (16 files); destination gradients re-tinted; the theme audit re-pinned to the new hexes with lavender itself on the retired list. Recorded as a trial: the owner asked to "see it overall". | Owner (2026-07-29) supplied the exact hex set and both gradient ramps: "renk paletlerini bu renkle günceller misin, bi genel değişelim görmek istiyorum". | Reverting to lavender, changing any given hex, or putting white text on the gradient. |

| D-044 | 2026-07-29 | **The rendevuu palette went dark, to the designer's screens.** The ground is night navy (#12162E) with panel cards (#1C2242); near-white ink reads, the pinks fill and mark, and the theme audit re-derived every ratio on the new ground (danger and onDanger recomputed for it). To the mocks, pixel-faithful: gradient onboarding progress bar; the birthdate step's own digit keypad (system keyboard suppressed); gender rows with per-answer glyphs and check badges; orientation rows as outlined pills; show-me as the three icon cards; interests as icon chips whose selected state is the brand gradient with the count on the button; Settings wears the gradient avatar ring with the crown only when Premium is actually true; Tatilim/Keşfet/Gelen kutusu empty states carry the mock's neon crops (dark-hotel-disc/pin, 3D chat). Known departures, all D-041-style honesty: no inbox unread dot, gradient labels stay dark ink (the gold end cannot carry white), and the crown appears only for real premium. | Owner (2026-07-29): "yeni tasarımlar geldi birebir uygula… ikonlar dahil her şeyi birebir geçir." | Reverting to the light ground, changing given hexes, or drawing any of the deliberately-omitted controls without their feature. |

| D-045 | 2026-07-29 | **The Figma glass system is the app's skin.** From the approved "Vacation Match — Flows" file: a `glass` token trio (6% white fill, 14% light edge, a strong variant) worn by every card, input, chip, choice row, chat card, tab bar and hero panel — read as frosted glass against the night navy without a native blur pass (a deliberate performance trade; expo-blur can be added later if a real blur is wanted). With it, the Figma accents: green "Açık" state chips, gold tracked feature plates, gradient own-chat-bubbles with dark ink, gradient face rings and heart badge on the match screen, a gradient like button in the deck, and the glass trust card on welcome. | Owner (2026-07-29): "tüm ekranları uygulamaya geçir birebir Figmadan", after approving the Figma flow file. | Changing the glass values, adding real blur, or introducing any surface that skips the tokens. |

| D-046 | 2026-07-29 | **The ground became a sunset.** The owner read the flat navy as "developer dark"; per their reference, every screen now stands on an indigo→violet→coral gradient (`backgroundGradient`, base #2A2350) painted by Screen/Onboarding/Welcome, with the floating tab bar's dock wearing the gradient's warm end — which also kills the white band that showed behind the bar (the navigator's default white peeking through a transparent dock). The bar itself is light glass over the warmth. Panels lifted a step (#3A3168); every ratio re-derived in the audit. Tatilim's "Hızlı seçenekler" section removed outright at the owner's word. | Owner (2026-07-29), with device screenshots and the sunset reference: "arka mavi çok dark oldu… bg bu olsun, navbar gibi cam olabilir mi." | Reverting to a flat ground, changing the gradient stops, or reviving the quick-options strip. |
| D-047 | 2026-07-29 | **Hotel search is global, and destination-first.** The `countrycodes=tr,cy` lock left the `hotel-search` function: Nominatim is asked about the whole world, because the person choosing a hotel is planning a *destination* — someone in Germany books a Türkiye hotel from their sofa, and the destination lives in the query ("Antalya", "Hilton Berlin"), never in where the phone is. Deliberately no viewbox, no proximity bias and no IP filter on this search: text relevance and Nominatim's importance rank the world, the result card's city/country line disambiguates, and the "Otel adı veya şehir" placeholder teaches the escape hatch for ambiguous brands. Location stays what Çevremde is for. Hits without a country are dropped before the catalogue (its own constraint requires one); the Türkiye fallbacks in city/country derivation left with the lock. Scale note recorded with the decision: the public Nominatim endpoint's 1 req/s policy fits the pilot; a global launch moves this function to a self-hosted or ODbL-compatible commercial endpoint, and ranking then adds declared-context and catalogue-popularity signals ahead of any locale hint. | Owner (2026-07-29): "uygula bu önemli bir feature", after the Germany-user-books-Türkiye scenario. | Reintroducing a country lock, adding IP/proximity bias to hotel search, or letting Çevremde's location logic leak into it. |
| D-048 | 2026-07-29 | **The room is geometry, not a data row.** `checkins.venue_id` was a required reference to a catalogue row, so a spot no provider had mapped meant no room and no feature — the owner's cases were a concert on a wooded island (Sziget, 150k people, 0 catalogue rows verified live) and standing inside an Espressolab that the map knew but the list had dropped. The anchor of last resort therefore stops being data and becomes arithmetic: `app.cell_of` snaps a reading to a ~200 m cell, `checkin_here` writes that cell through the one provider boundary as `provider='cell'`, and it cannot fail for want of a place. It is D-039's "where there is no venue, the neighbourhood is the venue", one step smaller and guaranteed. What a cell must never do is speak: its provider key is positional and `hotels.location` may never be revealed (D-005), so the name is the fixed placeholder `(cell)`, cells are excluded from the around-you list and from text search, `my_checkin` and the deck both return null for it, and the screen says where-you-are in the reader's own language. Rooms are matched by distance (1 km), never by cell equality, so a grid edge cannot split two people standing together. The privacy ledger improves rather than regresses: what is kept is a ~200 m square, coarser than the 20 m café the old model named. | Owner (2026-07-29): "yakınımda kısmının kesin sonuç vermesi gerekiyor — insanlar bunu konser alanı beachlerde kesin kullanacak", then "çekirdekten başla uygula". | Making a check-in depend on the catalogue again, returning a cell's name/key/city to any caller, offering cells in the list or in search, or matching a room by cell equality. |
| D-049 | 2026-07-30 | **A second provider, and the three things the sweep got wrong.** (a) *Overture Maps* joins as `provider='overture'`: OSM's coverage of small commercial venues is thin exactly where this product needs it (the owner's own café was missing from both OSM and, as it turned out, Overture), while the sources that have that coverage forbid the architecture — Google Maps terms permit caching a place id and little else, require display on a Google map, and forbid a derived database, and `hotels` *is* a derived database months of matches join to. Overture's CDLA-Permissive licence allows storing. It is a GeoParquet dataset on S3, not a lookup service, so it arrives through `scripts/overture-ingest.py` (DuckDB over HTTP range requests) as a deliberate pre-fill of a region, not a per-request call. Category mapping is exact-name, never substring: the first draft filed a hairdresser as a bar (`barber`) and an events agency as a concert hall (`event_planning`), so every name was read off the real taxonomy. Cross-provider deduplication is explicitly *not* done here. (b) *The vocabulary widened* with an eighth kind, `venue`: every big Istanbul concert venue was in OSM and every one was refused — Volkswagen Arena is `leisure=sports_centre`, Küçükçiftlik Park `tourism=theme_park`, the açıkhavas `amenity=theatre`, Bostancı `amenity=arts_centre`. "Konsere gittim, mekân yok" was our answer, not the map's. (c) *The sweep stopped truncating arbitrarily*: `out center 30` kept Overpass's own internal order, so 138 of the 168 venues inside 500 m of a dense street were discarded — including the café the owner was standing in. The radius is now the only bound; results are sorted by distance and cut at 40. (d) *A sweep that fails says so*: the public Overpass endpoint 504s in ordinary use (twice while investigating), and the function used to swallow it and draw an empty street, then name the neighbourhood as though nothing were there. It now returns 503 and the screen offers the sweep again — while checking in where you stand (D-048) keeps working, because it never depended on the sweep. The hardcoded "Türkiye" left with it (D-047). | Owner (2026-07-30): "overture entegrasyonunu yap ve o üç düzeltmeyi de aynı geçişte hallet", after "google apı konum konusunda en güvenilir olanı değil mi". | Substring category matching, cross-provider dedup without its own decision, capping a sweep by anything but distance, treating a failed sweep as an empty world, or putting a place source in the request path that forbids storing what we store. |
| D-050 | 2026-07-30 | **Google Places: rejected, on unit economics.** Not on licence grounds — a compliant shape does exist (store only `place_id`, which Google exempts from caching limits indefinitely, keep our own cell as the anchor, and fetch the name on demand for the owner of the check-in alone). It is rejected because the action it would pay for is the wrong kind of action: checking in where you are sitting is among the app's *most frequent*, and Çevremde is the free tier, so every request is pure cost against zero revenue. At $32 per 1,000 Nearby Search requests, a user opening the screen five times a day costs ~$4.80 a month — on a free feature. The Sziget weekend (150k people, ~3 opens each) would cost ~$14,400; the same weekend costs nothing extra under the present design. The decisive property: our costs scale with **geography** (one Overture ingest per region, then unlimited check-ins) while a per-request provider scales with **usage**. Licence and economics point the same way, which is not a coincidence — the sources that let you store are the ones that do not meter you. The coverage gap is closed instead by the Overture ingest (D-049), first-party seeding for pilot areas, and the here-anchor (D-048) which needs no provider at all. | Owner (2026-07-30): "adam oturduğu mekânı check-in etmek için servise nasıl para ödüyoruz", then "evet reddedildi olarak yaz gerekçesiyle". | Reopening this without new information. If it is ever reopened, the case must show what the tail is actually worth after an Overture ingest has been measured, and it must not put a metered call on a free, high-frequency action. |
| D-051 | 2026-07-30 | **Two searches, because they were never one question.** `search_hotels` served both the trip tab ("which hotel am I staying at") and Çevremde's name fallback ("what is this place I am sitting in") over one table with no filter on kind. While the catalogue was ~100 rows the noise was invisible; at 1,075 it was not — searching "sorgun" on the *hotel* tab returned Sorgun Beach Club and Sorgun Sahil Bar and no hotel at all, and "marina" returned four restaurants and a beach. The matching logic moved into `app.search_places(query, limit, lodging_only)`; `search_hotels` asks it for lodging and the new `search_venues` asks it for anywhere a person can be, hotel lobbies included. The `hotel-search` edge function takes a `mode` so both keep the same OSM enrichment. Rows predating the kind vocabulary (D-041) were backfilled to `hotel` after sampling proved they are exactly that — Rixos, Hilton, Cratos, Kempinski and the seeded pilot hotels. | Owner (2026-07-30) called it before the numbers did: "uygulamanın algoritması bozulacak şimdide", then chose to keep the ingested rows and fix the filter. | Serving both questions from one unfiltered search again, or letting the trip tab answer with a café. |
| D-052 | 2026-07-30 | **Google Places enters, for one screen, and never becomes our catalogue.** Reversing part of D-050: Google is admitted to feature three only — the moment somebody presses check-in — and is barred from hotel search and from Oteldeyim, which stay on the catalogue and on PostGIS. The order on that screen is fixed: (1) our own Overture/OSM catalogue, searched against the user's GPS; (2) a written search when they cannot see their place; (3) a separate, clearly-labelled Google list behind "Google ile daha fazla mekân ara" carrying the required attribution; (4) the cell anchor (D-048) as the last resort, never the main path. **What is stored depends on which provider answered.** An Overture pick is a catalogue row and its id, name and location persist as they always have — those licences (CC0 / CDLA-Permissive / Apache) permit it. A Google pick stores exactly three things: `google_place_id`, our own check-in cell, and `expires_at`. The name is never written down; it is resolved live from the Place ID and deduplicated in session memory. Explicitly rejected, and it was my proposal: using Google's `displayName` + `location` to find or mint a matching Overture row. Two reasons — it is fairly read as producing new content from Google Content, which the terms restrict, and it is technically unsound, since one Istanbul bounding box in our own ingest held **forty** rows named "Espressolab" and a name-plus-proximity match would bind the wrong branch. Cost is bounded by construction rather than by hope: calls go through the backend, the API key is restricted to it, an application counter stops at 4,500 a month, and the Cloud daily quota sits near 150 — the ceiling deliberately under the real budget because metering differs. When the quota is gone the screen keeps working on the catalogue, the written search and the cell. | Owner (2026-07-30), with the architecture spelled out: "katalogla otomatik eşleştirmeyi şu haliyle onaylamıyorum… Overture seçimi kalıcı Overture mekânı olsun; Google seçiminde yalnız place_id, kendi check-in hücremiz ve expires_at saklansın." | Persisting a Google name or coordinate, deriving a catalogue row from Google Content, putting Google in the hotel or Oteldeyim paths, calling it anywhere but on a check-in press, or removing the counter and the quota. The earlier "83 users" figure is not a threshold — the real one comes from measured check-ins per user. |
| D-053 | 2026-07-30 | **No Google Nearby. A written advanced search, entitled and rate-limited.** Refining D-052 the day it landed: Nearby is removed entirely, because by the time Google is reached the user has already *typed* the name — "what is around me" is our catalogue's question and "find me this name" is a text search's, and Nearby sat between the two while costing the most (32 $/1000 against Autocomplete's 2,83). The order is: catalogue list → written search in our own catalogue → "Google ile gelişmiş ara", which sends the typed name plus the current location so the right branch surfaces. We ask for `place_id` and the name and nothing else — Google's coordinate is never needed, because the anchor is our own cell. **Two counters, doing different jobs**: the entitlement (3 a month free, 10 premium) counts *finds* and is spent only when a Google-labelled check-in completes, which is what makes it fair — a search that found nothing costs the user nothing; and a per-user search rate limit (10 an hour) counts *attempts*, which is what actually bounds spend, since an entitlement alone would let somebody search fifty times and select nothing at our expense. The monthly service ceiling from D-052 sits above both. When the entitlement is gone, the catalogue, the written search and the here-anchor all keep working. **Overture is synchronised monthly, not loaded once**: releases are monthly, so a one-off load is stale within a quarter. The sync updates changed rows and corrects moved ones through the existing upsert, and rows absent from the new release are set `is_active = false` rather than deleted — `checkins` and `user_active_hotel` hold `on delete restrict` and matches reference a venue for months, so deleting would break history while deactivating merely stops offering it. | Owner (2026-07-30): "Google Nearby yok. Overture/OSM ana liste; Google yalnız kullanıcı mekânını bulamadığında açılan, 3/10 hakla sınırlı yazılı gelişmiş arama." The rate-limit number (10/hour) and the monthly reset are the assistant's recommendation, taken because the owner's reply returned unchanged; both are one-line changes. | Reinstating Nearby, spending an entitlement on a search that found nothing, letting searches run without a per-user limit, deleting a venue during a sync, or asking Google for a coordinate we do not use. |
| D-053a | 2026-07-30 | **Corrections to D-053 before any key is configured.** The first implementation diverged from the approved brief in two ways that mattered. It called `places:searchText` with a location *bias*; the brief specifies **Autocomplete (New)** (`places:autocomplete`) with `input`, a per-session `sessionToken`, and a 1,500 m `locationRestriction` — a different and cheaper SKU, and a restriction rather than a hint. And it accepted `p_google_place_id` straight from the client, which made a forged label possible: the label is shown to other people, so anybody could have claimed to be anywhere. A selection is now recorded server-side from what Autocomplete actually returned, bound to the user who searched, single-use and short-lived, and `checkin_here` takes only that token. Also corrected: separate calendar-month ceilings per operation (Autocomplete 9,000, Place Details 4,500) where one counter had hidden two very different prices; a three-character minimum; and the rolling session limits (10/hour, 30/24h) enforced server-side alongside a twelve-request per-session cap and a three-minute idle expiry. **The `$65/month at 100k users` figure is withdrawn** — the brief is explicit that it conflates sessions with upstream requests, omits abandoned-session pricing and the tier of any terminating Place Details call, and the decision does not rest on it; metering plus per-operation ceilings is the protection. | Owner (2026-07-30), with the D-053 brief: "places:searchText kullanımını tamamen kaldır… checkin_here rastgele istemci Place ID'sini kabul etmesin." | Reinstating Text Search or Nearby, accepting a client-supplied Place ID, merging the two ceilings, or recording a cost forecast as an accepted estimate. |

| D-053b | 2026-07-30 | **The production-safety half of D-053, built before the key exists.** Seven items the first pass had named as deferred, in the owner's priority order. (1) *Real request metrics*: `app.provider_events` records one row per Google attempt or refusal — an operation, an outcome (`ok`/`empty`/`error`/`deduplicated`/`refused_session`/`refused_ceiling`), a user and a session, and deliberately no query text, no coordinate and no display name. Measurement is never awaited for correctness: a failed write must not fail a request. (2) *Deduplication*: `app.query_fingerprint` normalises case and whitespace, `search_sessions.asked_hashes` remembers the last 24 in a session, and a repeat is answered `duplicate` **before** the request cap is charged and without any upstream call — the caller keeps the predictions it already has, which is why none of them ever had to be stored. (3) *One active session per user*: opening a session closes any other of that user's as `abandoned`, so a client that leaks session ids cannot run several meters at once. (4) *Outcome measurement*: a session ends `abandoned`, `empty`, `failed` or `converted`, and `checkin_here` marks `converted` inside the same transaction as the check-in — so search-to-check-in is a fact rather than an inference. (5) *Ceiling exhaustion is tested*: a spent monthly ceiling closes the Google door and nothing else. The catalogue list, the written search and "Buradayım" keep working, the refusal happens before the paid request rather than after it, an unreachable counter is itself a refusal (failure closed), and **a search we could not make spends none of the user's entitlement** — our ceiling is our problem. (6) *The disclosure exists* in Settings in both languages, naming what is stored (a Place ID, never Google's name), what is not (any coordinate from Google, ever), the three-hour check-in expiry, and that Google is called only on a press. (7) *Sync metadata*: `hotels.source_release`/`last_seen_at`/`deactivated_at` plus `app.sync_runs`, and retirement now takes a **run** rather than a box — so a download that dies halfway cannot quietly retire a city. `deactivate_unseen_places` refuses a run that is not `complete`, refuses to touch a provider the run did not declare, and retires only inside the box the run declared. No cost forecast is produced; the metrics exist so that one can eventually be measured rather than guessed. | Owner (2026-07-30): "D-053'ün ertelenen üretim güvenliği işlerini tamamla… Maliyet tahmini üretme. Önce ölçüm altyapısını tamamla ve test et. Mevcut D-053 davranışlarını, hak sistemini ve fallback yollarını bozma." | Charging a user's entitlement for a refused or failed search, retiring places from a run that is not complete, running more than one live search session per user, recording query text or coordinates in the metrics, or publishing a cost estimate before the metrics have run against a real key. |

| D-054 | 2026-07-30 | **The vacation venue is chosen destination-first, from Google, and Google's answer is never written down.** The owner's brief replaces the catalogue hotel search on the trip tab with two Google Autocomplete (New) steps: *"Nereye gidiyorsun?"* accepts only a geographic prediction (locality, sublocality, neighbourhood, administrative area, island — never an establishment, and never restricted to formal cities, or Alaçatı and Dubai Marina would both be refused), and *"…'da nerede olacaksın?"* searches inside that destination's own Google viewport. **The default venue mode carries no type restriction**, because a hotel-only mask is exactly what hides a beach club: Google files Before Sunset under `bar`, and the named public beaches under `beach` or `tourist_attraction`. The optional chips are ours — `Tümü` (no mask), `Konaklama` (lodging types), `Beach & Club` (a wide set that accepts every way Google classifies a beach club) — and `Tümü` makes one request, not three. **Venue identity is `(provider='google', provider_place_id)`**, which the `hotels` table's existing unique key already expresses, so two people who pick the same Place ID land in the same room and two same-named places with different IDs do not. What is stored is the Place ID, our own venue UUID, our own `venue_kind` (read off the chip the user searched under, never off Google's types), membership, dates, verification outcome, matches, chat. What is *not* stored is Google's display name, address, photo, rating, coordinate or viewport — `hotels.location` becomes nullable for this one provider, the name is a `(google)` placeholder resolved live from the Place ID for each screen that draws it, and a destination's viewport lives on the search session and is purged with it. `HERE_NOW` therefore moves its authority to the backend: the edge function resolves the venue's current coordinate from Google at check time, the 500 m test (D-002) still runs in PostGIS, and neither the coordinate nor the distance is persisted or returned. This is a **core** flow, so the D-053 3/10 monthly entitlement deliberately does not attach to it (§6); spend is bounded by a 3-character minimum, a 350 ms debounce, one in-flight request per field, stale-response rejection, per-session Google tokens, the existing per-user session limits and the per-operation monthly ceilings. | Owner brief, 2026-07-30 (`GOOGLE_DESTINATION_VENUE_IMPLEMENTATION_BRIEF.md`), which is explicit that the flow is no longer hotel-only and that selecting Alaçatı and then searching an Alaçatı beach must work. It reverses the boundary D-050/D-052 drew — "Google is barred from hotel search and from Oteldeyim" — on a different question than the one those decisions answered: they rejected a metered call on a *free, high-frequency* action (checking in where you sit), and choosing where you will spend a holiday is neither. D-052/D-053's rejection of a Google-derived catalogue is **not** reversed and is what the no-persisted-content rule keeps. **Three consequences recorded rather than hidden:** (a) a Google venue has no stored coordinate, so it cannot join the D-038 15 km region pool — its members see their own room and are not offered as neighbours until a venue coordinate exists that is ours to keep; (b) another user's card can only be labelled with a venue name the *viewer's* own session resolved, so a Google venue's name costs a Place Details call per screen rather than a column read; (c) Overture/OSM stays exactly where it is for Çevremde (D-039/D-048/D-049) and for every venue already chosen — nothing is deleted, and an existing active hotel keeps working untouched. | Putting a type restriction on the default venue search, persisting any Google display content, attaching a paid-entitlement gate to destination or venue selection, accepting a client-supplied Place ID or coordinate as authoritative, or letting a provider outage delete a room, a match or a chat. Reopening (a) needs its own decision about where a Google venue's coordinate may come from. |
| D-054a | 2026-07-30 | **Two corrections the live key found, both to my own first design.** (a) *The destination's box is padded.* Google's viewport for a sublocality is the built-up outline of the town, and holidays happen just outside it: with a strict `locationRestriction`, staging answered the brief's own Scenario A — "Alaçatı, then Biblos" — with a currency exchange and a guesthouse, and no resort at all; "Before Sunset" and "Ilıca Plajı" returned nothing. The box is now padded by half its own span, floored at ~5.5 km and capped at ~28 km per side, with the longitude pad scaled by latitude. The brief anticipates this and permits "a defensible derived search area". All three of the brief's cases now answer correctly from Alaçatı, and the Marbella Biblos still does not. (b) *The `Beach & Club` chip is removed.* Built exactly as §4 describes — `beach`, `bar`, `night_club`, `restaurant`, `tourist_attraction` — it returned **nothing** for Before Sunset on staging, while the unrestricted default returned it first. `includedPrimaryTypes` caps a request at five, and Google's primary type for a beach club is not reliably any of them; the brief's own warning that beach clubs are classified inconsistently is, in the end, an argument against the chip rather than for a wider one. So the chips are `Tümü` (no restriction, the default) and `Konaklama` (lodging, the one category Google's types are reliable about). A control named after the thing it hides is the D-041 rule's exact prohibition. | Measured on staging against the real key, 2026-07-30, before any of it was believed. | Reintroducing a type mask on the default search; making the pad unbounded, which would let a destination reach into its neighbours; or reviving a category chip without evidence that its mask actually returns the places it names. |

| D-055 | 2026-07-30 | **A Google venue learns roughly where it is from our own users, a neighbour's name costs at most three calls a deck, and the cost is measured rather than guessed.** The owner's ruling closing V-010, V-011 and V-012. (a) *The coarse region cell.* Neither Google's coordinate nor anybody's raw GPS is stored. When a Here Now verification succeeds at a Google venue **and** the device reported a fix accurate to 100 m or better, the reading is snapped to a ~1.5 km cell inside the same transaction and discarded; the cell is written to `hotels.coarse_region_cell` — venue-level, app-owned, never on a user row and never in the event log. One contribution per person per venue, insert-only, so no amount of repeat checking moves a venue. A rival cell needs two distinct people *and* strictly more of them before it may replace the incumbent, which is the "outliers are not accepted" rule. `coarse_region_point` is a GENERATED column derived from the key, so no code path can ever put a real coordinate there. The cell is used for one thing — candidacy in the D-038 fifteen-kilometre pool — and explicitly not for the 500 m check, which still measures against the coordinate the backend resolved from Google a moment earlier. Nothing shows a user a cell, a coordinate or a distance; a neighbour is "çevrede" and nothing more. A Google venue with no cell keeps its own room and is simply not offered as a neighbour. (b) *Labels are bounded.* No Place Details call per render: the viewer's own venue is never resolved, a Place ID is resolved at most once per deck session however many cards carry it and however often the deck reloads, at most three distinct labels are resolved per deck session, the answers live in memory alone, and a ceiling, a provider failure or a fourth venue all fall back to the generic "nearby". No card, swipe or match ever waits on a label. (c) *Measurement, not forecast.* Eight counts — venue selections, Here Now verifications, cell formation and its three refusal shapes, region-pool participation, unique Place IDs and Details calls per deck session, generic fallback rate, and both hard-ceiling usages — surfaced by `venue_operations_view()`. No cost estimate is produced and no quota is raised. | Owner (2026-07-30), closing the three items D-054 left open, with the privacy rules written out rather than delegated. | Storing Google's coordinate or a raw reading; putting the cell on a user row or in the metrics; letting one person move a venue; using the cell in the 500 m check or showing it to anybody; resolving a label per render; raising a ceiling before real telemetry has been read. |

| D-055a | 2026-07-30 | **Two security corrections, before events.** (a) *A 900 m fix does not pass a 500 m check.* V-010 used accuracy only to decide whether a reading could **teach** a venue its cell; the check itself still accepted anything, so a reading whose error was nearly twice the radius could open the room the radius exists to protect. `app.reading_problem(lat, lng, accuracy)` is now the single validator — no stated accuracy is a refusal, worse than 100 m is a refusal — and **both** presence paths call it, as the event room will. A refusal is its own outcome, `LOCATION_INACCURATE`, not a quiet "not here": it writes no presence answer, no entitlement, no contribution and no success, leaves the previous answer standing, and says "Konum yeterince hassas değil. Açık alanda tekrar dene." The database is the authority; the client copy of the ceiling exists only for the wording, and the edge function's copy only to skip a paid Place Details call it already knows will be refused — the gates still run in their usual order, so a free member with a bad fix is told about Premium rather than about their GPS. 99 m passes, 101 m and 900 m and a silent device do not, each asserted. (b) *A contribution is no longer a location record.* `venue_region_votes(venue_id, user_id, cell_key)` said, in one row, where one named person had been to within 1.5 km — a shape far beyond its purpose, which was only ever "has this person already contributed here". It is replaced by `venue_region_contributors(venue_id, contributor_key)` and `venue_region_tally(venue_id, cell_key, contributions)`. `contributor_key` is an HMAC of venue and user under a server-side secret: irreversible, and salted per venue so one key cannot follow somebody between venues. The contributors table has no cell and deliberately no timestamp. **No row anywhere pairs a user with an observed location**, and a schema test asserts it by column-name shape — cell, geohash, h3, region_point, viewport — so a future column is caught before it is granted. Existing contributions were converted in the migration rather than discarded; the counts were real, only the linkage was wrong. `coarse_region_cell`, `coarse_region_point` and all three backing tables are unreachable by `anon` and `authenticated`, proven against staging as well as in pgTAP. | Owner (2026-07-30), reviewing D-055 before the events milestone. Both were mine to have got right the first time: the first mistook "good enough to teach" for "good enough to trust", and the second let a table's shape outrun its purpose. | Loosening the accuracy ceiling, letting either presence path validate a reading its own way, granting any client a location-derived column, or reintroducing a row that holds a person and a place together. |

## Open owner decisions

- **Should the Upcoming room require overlapping stay dates?** Today it does
  not: anyone with a stay at that hotel that has not ended yet is in the room,
  so two people staying six months apart can match. Filtering by overlap is
  what most people would expect, but it shrinks an already thin pilot pool,
  and pool density is the thing most likely to make the first hotel feel dead.
  Left broad on purpose; one `where` clause changes it. (Resolved 2026-07-27: D-035 added that clause.)
- **Suspension is per account, not per person.** A suspended user can sign up
  again with another phone number — and, since H2, can delete the suspended
  account first, so nothing is even left behind. Closing that needs device or
  stronger identity signals, which is a bigger privacy decision than the MVP
  should make on its own. Worth sizing honestly before the pilot.
- **A coordinated pile-on can force a moderation flag.** Three distinct
  reporters raise a `FLAGGED` row automatically (D-013), and three disposable
  addresses are not hard to get. What it cannot do is suspend anyone —
  `resolve_report` is service_role only, so a human still stands between the
  flag and any consequence. Disposable phone numbers cost more than disposable
  email addresses but do not eliminate this risk. The flag is queue priority,
  not a ban. Whoever
  watches the queue during the pilot should know it is a lever.
- **Fake profiles: proposed and advised against (2026-07-28).** The owner
  floated seeding ~10 fake women's profiles into thin Upcoming rooms so the
  app never looks empty before a premium purchase. Studio assessment, given
  in full in chat: this is the FTC-v-Match / Ashley Madison fact pattern —
  deceptive commercial practice (no force required, influencing the purchase
  decision suffices), store-policy ban territory, KVKK/personality-rights
  exposure on the photos, and commercially self-defeating (likes into a void
  → refunds and one-star reviews; the hotel setting makes the fabrication
  physically checkable). Recommended instead, awaiting owner go: premium
  sales stay closed below a room-density threshold ("founding member" free
  premium until then) and the empty room becomes an invite mechanic. Fake
  profiles are not built and should not be.
- **Room access pricing after D-039.** The owner floated making both hotel
  rooms premium-only once Çevremde exists as the free layer. Studio warning,
  standing: free members are the inventory premium buyers pay to meet —
  paywalling room *entry* empties the rooms. Current shape (free limited
  entry, premium lifts limits + Here Now) is the studio recommendation until
  pricing is decided with L-001.
- First pilot city and hotels.
- Whether existing chats expire after the trip.
- Premium package and price in the next phase. (2026-07-28: the entitlement
  and its rules exist — D-036 — but purchasing does not; price, package and
  store billing remain open.)
- **Premium direct message (D-036 follow-up).** The owner's rule includes
  "premiumsa doğrudan mesaj atabilir" — chat without a mutual match. That
  needs its own model (a pre-match conversation the other person can refuse),
  safety rules, and screens; staged as the next premium slice, not smuggled
  into this one.
